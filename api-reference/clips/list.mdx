---
title: "List clips"
openapi: "openapi/akapulu.json GET /clips"
---

List clips on the account, newest first.

Each row includes `id`, `name`, `status`, and download URLs when the files exist. Phonemes and overlay flags are on [Get clip](/api-reference/clips/get).

How a clip job works is in [Overview](/guides/clips/overview).
