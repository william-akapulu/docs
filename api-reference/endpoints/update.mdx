---
title: "Update Endpoint"
openapi: "openapi/akapulu.json POST /endpoints/update"
---

Update an HTTP endpoint you own. The request must include `id` plus the full `name`, `url`, `headers`, and `body` fields (same shape as create).
