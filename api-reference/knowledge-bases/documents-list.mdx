---
title: "List Documents"
openapi: "openapi/akapulu.json GET /knowledge-bases/{corpus_id}/documents"
---

List documents in a knowledge base you own.

`status` is `pending` while embedding is still running, then `completed`.
