---
title: "Update Knowledge Base"
openapi: "openapi/akapulu.json POST /knowledge-bases/update"
---

Update a knowledge base's `name` and `description`.

Required: `id` and `name`. Optional: `description` (400 characters or fewer).
