---
title: "Custom UI"
description: "A Next.js example using Akapulu's headless React hooks for a fully custom UI."
---

Use this example when you want full control over the conversation experience while still building on the Akapulu Web SDK.

## What this example shows

- **`AkapuluProvider`** and headless hooks from [`@akapulu/react`](/web-sdk/react-sdk)
- **`useAkapuluSession`** for lifecycle and store state; **`useAkapuluMediaControls`** for mic/cam; **`useAkapuluEvents`** for tools and other events; **`AkapuluBotAudio`** for assistant audio.
- **`@daily-co/daily-react`** (**`DailyVideo`**, **`useDaily`**, **`useParticipantIds`**, **`useVideoTrack`**) for remote/local video under the same provider.
- **[`@akapulu/server`](/web-sdk/server-sdk)** in route handlers so the API key never ships to the browser.

**Connect / updates in this app**

- **`POST /api/akapulu/connect`** and **`GET /api/akapulu/updates?conversation_session_id=...`** 

**After the call ends**

- Opens a **post-call review** screen in the example app.
- Conversation data comes from **`getConversationDetail`** and **`getConversationRecording`** on **[`@akapulu/server`](/web-sdk/server-sdk)** (metadata + transcript rows; recording as redirect / JSON / binary).

## View on GitHub

[custom-ui](https://github.com/Akapulu/akapulu-examples/tree/main/fundamentals/custom-ui)
